include:
  - package:flutter_lints/flutter.yaml
  - analysis_options_base.yaml

analyzer:
  errors:
    invalid_annotation_target: ignore
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "server/**"
    - "third_party/**"
